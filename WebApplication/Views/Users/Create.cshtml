@model WebProject.Models.Users

@{
    ViewData["Title"] = "Create";
}



@{
    ViewData["Title"] = "CookAndBuy";
}

@section Styles{
    <link rel="stylesheet" href="~/css/Login.css" />
}
<style>
</style>
<div id="login-box">
    <form asp-action="Create">
        <div asp-validation-summary="ModelOnly" class="text-danger"></div>
        <div class="left">
            <h1>הרשמה</h1>
            <div class="form-group">
                <input type="text" name="email" placeholder="הכנס מייל" />
            </div>
            <div class="form-group">
                <input type="password" name="password" placeholder="הכנס סיסמא" />
            </div>
            <input type="password" name="password2" placeholder="ודא סיסמא" />
            <div class="form-group">
                <input type="submit" name="signup_submit" value="Create" class="btn btn-primary" placeholder="הירשם" />
            </div>
        </div>
        <div class="right">
            <span class="loginwith">Sign in with<br />social network</span>

            <button class="social-signin facebook">Log in with facebook</button>
        </div>
        <div class="or">OR</div>
    </form>
</div>



<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
